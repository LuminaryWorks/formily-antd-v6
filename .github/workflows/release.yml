# Deprecated — do not use
#
# This Formily upstream release workflow expects NPM_AUTH_TOKEN + lerna and is
# incompatible with LuminaryWorks OIDC Trusted Publishing.
# Use publish-packages.yml instead (see PUBLISH.md).
#
# Kept disabled so accidental "chore(release)" commits cannot publish with a
# missing/legacy secret.

name: Release (deprecated)

on:
  workflow_dispatch:
    inputs:
      acknowledge:
        description: Type "deprecated" to acknowledge this workflow must not publish
        required: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Use .github/workflows/publish-packages.yml (OIDC)."
          echo "See PUBLISH.md"
          exit 1
